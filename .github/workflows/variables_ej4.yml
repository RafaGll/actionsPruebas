name: Ejercicio 4 Variables y Outputs
on: 
  push:
    branches:
      - main
      - develop
jobs:
  json_read:
    runs-on: ubuntu-latest
    steps:
      - name: clonar repo
        uses: actions/checkout@v4
      - name: Leer json
        run: |
          echo 'PACKAGE_JSON<<EOF' >> $GITHUB_ENV
          cat ./archivo.json >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV
      - name: Guardar versión
        run: echo "version=${{ fromJson(env.PACKAGE_JSON).applications.verssion }}" >> $GITHUB_ENV

      - name: main 
        if: ${{ github.ref_name == 'main' }}
        run: echo "Desplegando versión ${{ env.version }} en producción"

      - name: develop
        if: ${{ github.ref_name == 'develop' }}
        run: echo "Desplegando versión ${{ env.version }} en develop"

